import 'package:flutter/material.dart';

class MyUpvotes extends StatelessWidget {
  const MyUpvotes({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("My Upvotes"),
      ),
    );
  }
}
