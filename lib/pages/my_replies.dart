import 'package:flutter/material.dart';

class MyReplies extends StatelessWidget {
  const MyReplies({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("My Replies"),
      ),
    );
  }
}
